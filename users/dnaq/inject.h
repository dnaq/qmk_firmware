switch (combo_index) {
    case lsftSpcCapsWord:
    case lsftRsftCapsWord:
        if (pressed) {
            caps_word_enable();
        }
        break;
        if (pressed) {
            caps_word_enable();
        }
        break;
}
