switch (combo_index) {
    case lsftRsftCapsWord:
        if (pressed) {
            caps_word_enable();
        }
        break;
        if (pressed) {
            caps_word_enable();
        }
        break;
}
